<%= form_for(@test_class) do |f| %>
  <% if @test_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_class.errors.count, "error") %> prohibited this test_class from being saved:</h2>

      <ul>
      <% @test_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :string %><br />
    <%= f.text_field :string %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
